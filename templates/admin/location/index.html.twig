{% extends 'admin/base_admin.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('dashboard')}}
{% endblock %}

{% block title %}Location{% endblock %}

{% block body %}
    <main>
        <div class="card">
            <h3 class="card-header text-center font-weight-bold text-uppercase py-4" >Location</h3>
            <div class="card-body">
                <table class="tableau table table-striped table-hover table-bordered">
                    <thead>
                    <tr>
                        <th style="width: 75%;">Ville</th>
                        <th>Code Postale</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for location in locations %}
                        <tr>
                            <td>{{ location.city }}</td>
                            <td>{{ location.postalCode }}</td>
                            <td>
                                <a href="{{ path('location_edit', {'id': location.id}) }}" class="btn btn-warning" style="margin-right: 5px;"><i style="color: white;" class="fas fa-edit"></i></a>
                                <a href="{{ path('location_delete', {'id': location.id}) }}" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                <div class="text-right mt-4">
                    <a href="{{ path('location_new') }}" class="btn btn-primary">Créer</a>
                </div>
            </div>
        </div>
    </main>
{% endblock %}

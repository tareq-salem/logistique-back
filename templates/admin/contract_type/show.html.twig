{% extends 'base.html.twig' %}

{% block title %}ContractType{% endblock %}

{% block body %}
    <h1>ContractType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Created_at</th>
                <td>{{ contract_type.createdAt ? contract_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modified_at</th>
                <td>{{ contract_type.modifiedAt ? contract_type.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ contract_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contract_type.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ contract_type.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contract_type_index') }}">back to list</a>

    <a href="{{ path('contract_type_edit', {'id': contract_type.id}) }}">edit</a>

    {{ include('admin/contract_type/_delete_form.html.twig') }}
{% endblock %}
